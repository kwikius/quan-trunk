language: cpp

env:
  - QUAN_ROOT="/home/travis/build/kwikius/quan-trunk" 	

addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - build-essential
    - ccache
    - g++-6.1
    - gcc-6.1
    
script: export CXX="g++-6" && make 

